# Phase 5 RED Test Note

The first Phase 5 CI run is intentionally expected to fail because the new `VertexProjectTests` target references project schema and deterministic codec types that have not been implemented yet. This records the TDD RED state before production behavior is added.
