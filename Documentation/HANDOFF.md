# Session Handoff

## Read this first

A new session must read, in order:

1. `README.md`
2. `Documentation/PRODUCT_VISION.md`
3. `Documentation/ENGINE_INVARIANTS.md`
4. `Documentation/ROADMAP_28_PHASES.md`
5. `Documentation/SOURCE_ADOPTION_MATRIX.md`
6. `Documentation/CORE_ARCHITECTURE.md`
7. `Documentation/MEDIA_IO_ARCHITECTURE.md`
8. `Documentation/GPU_RENDER_GRAPH_ARCHITECTURE.md`
9. `Documentation/PROJECT_PERSISTENCE_ARCHITECTURE.md`
10. `Documentation/PROJECT_TEST_MATRIX.md`
11. `Documentation/PHASE_5_COMPLETION.md`
12. `Documentation/PHASE_5_WORK_LOG.md`
13. `Documentation/VERSIONING_AND_ARTIFACTS.md`
14. `Documentation/BRANDING_AND_FIRST_RUN.md`
15. the newest files under `docs/superpowers/specs/` and `docs/superpowers/plans/`
16. open pull requests and the latest GitHub Actions results

## Current state

- Repository: `woo642778-art/After-Effects`
- Phase 1 branch: `agent/phase-1-source-audit`
- Phase 2 branch: `agent/phase-2-core-architecture-branding`
- Phase 3 branch: `agent/phase-3-media-io`
- Phase 4 branch: `agent/phase-4-gpu-render-graph`
- Phase 5 branch: `agent/phase-5-project-persistence`
- Current completed program: Phase 5 of 28
- Product version: `5.0.0 (5)`
- Project schema version: `1`
- Installed display name: `After Effects`
- Bundle identifier: `com.woo642778.aftereffects`
- Minimum OS: iOS 17.0
- Attribution: `Made by Maze`
- Binary policy: unsigned arm64 IPA generated by GitHub Actions
- Current app behavior: startup branding, one-time Telegram promotion, movie inspection, thumbnail and waveform extraction, native Metal still-frame Render Lab, byte-identical PNG output, project create/open/save/export, persistent Render Lab values, Undo, Redo, write-ahead journal, autosaves, recovery candidates, missing-media detection, relinking, and optional media embedding

## Final Phase 5 verification

- Product and CI source HEAD: `e68ffde2eebcdb58becc9d3d1ecaf195f1861e76`
- CI trigger-policy completion commit: `f563d0a524c51ff803bf8bd6b385c219bf76f3d9`
- Successful workflow run: `31069517329`
- Portable tests: 58 passed
- Native project-package tests: 9 passed
- Native Metal fixture tests: 2 passed
- Artifact ID: `8955148634`
- Artifact name: `After-Effects-5.0.0-unsigned-ipa`
- Artifact API digest: `sha256:f43efd3c17e018f17781f5e92cda888de6a223061e4703c136f7492659da1ea8`
- Extracted IPA SHA-256: `395e67c262fa24cb7f9b75f459ced1bf4673d6edbdc24d622d1456342f18d366`
- Executable: `Payload/AfterEffects.app/AfterEffects`, Mach-O 64-bit arm64
- Version: `5.0.0 (5)`
- Minimum OS: iOS 17.0
- `Assets.car`: present
- `Vertex_VertexRenderMetal.bundle/default.metallib`: present, 6,996 bytes

## Modules implemented

### VertexCore

- exact `RationalTime` with iOS 17-compatible checked arithmetic
- stable `VertexID`
- explicit geometry and color metadata
- deterministic dependency graph and structured errors
- active Phase 5 milestone and source-adoption record

### VertexMedia and VertexMediaAVFoundation

- portable media descriptors, requests, providers, cancellation, errors, images, and waveforms
- asynchronous AVFoundation metadata inspection
- requested-time thumbnail decoding and PCM waveform generation

### VertexRender and VertexRenderMetal

- portable semantic graph, validation, request, result, cache key, cancellation, and latest-request coordination
- native Metal transform, exposure, saturation, inversion, opacity, resizing, readback, PNG encoding, and timing metrics
- preview and PNG output continue to use the same returned bytes

### VertexProject

- deterministic schema version 1 project and manifest values
- portable media references and Render Lab settings
- sorted-key JSON, canonical UTC timestamps, and SHA-256
- project commands, inverse operations, revision and duplicate-command checks
- bounded Undo and Redo history with coalescing
- journal preparation before Undo or Redo mutation
- checksummed newline-delimited journal and idempotent replay
- future-schema inspection and sequential migration contracts
- strong fingerprint-based media relinking decisions

### VertexProjectFoundation

- secure `.aeproject` layout and package-relative path containment
- durable temporary writes and same-volume atomic rename
- project backup, manifest checksum, history persistence, and journal append
- current, previous, and two hourly autosaves
- non-destructive recovered-package creation
- backup recovery isolated from newer history
- bookmark adapters
- verified media embedding and embedded-media resolution

## Decisions already made

- Phase number equals product major version. Phase 6 may publish only `6.0.0 (6)` and `After-Effects-6.0.0-unsigned.ipa` after completion.
- Application version and project schema version are independent. Phase 5 uses app `5.0.0` and schema `1`.
- Internal engine modules retain the `Vertex` namespace.
- Project-neutral models may not expose AVFoundation, Metal, UIKit, SwiftUI, security-scoped URL objects, file descriptors, or absolute sandbox paths.
- External media references are the default; verified package embedding is optional.
- Filename matches alone never produce automatic media relinking.
- A command is journaled and synchronized before durable project mutation.
- `project.json` is never overwritten in place.
- Recovery preserves the damaged source package and creates a separate recovered package.
- Backup project data is not paired with newer current history.
- Preview and output continue to share one semantic render graph.
- MetalPetal and VideoIO remain audited but unlinked research candidates.
- MiniCut remains GPL behavioral reference only.
- Documentation-only changes do not rebuild the final artifact.

## Pull request stack

- PR #1: Phase 1 foundation, base `main`
- PR #2: Phase 2 core architecture and identity, stacked on Phase 1
- PR #3: Phase 3 media I/O and 3.0.0, stacked on Phase 2
- PR #4: Phase 4 GPU render graph and 4.0.0, stacked on Phase 3
- PR #5: Phase 5 project persistence and 5.0.0, stacked on Phase 4

Merge in numeric order. After each base PR merges, retarget or rebase the next PR and rerun its final verification before merging.

## Immediate Phase 6 actions

1. Use the brainstorming and design gate before code.
2. Define stable composition and layer identities and explicit ownership rules.
3. Define media, adjustment, null, guide, camera, light, and nested-composition project models without claiming unsupported rendering behavior.
4. Decide the schema 1 to schema 2 migration or prove schema 1 can be extended compatibly before persisting layer data.
5. Define layer ordering, visibility, lock, solo, guide, timing range, source reference, and nested-composition dependency rules.
6. Add project commands, inverse operations, journal records, Undo, Redo, migration, corruption, and deterministic-serialization tests for layer changes.
7. Extend the existing `VertexRender` graph instead of creating a second compositor.
8. Build one real multi-layer vertical slice with deterministic pixel tests and preview/output parity.
9. Publish `6.0.0 (6)` only after all Phase 6 tests, iOS Release build, IPA inspection, documentation, and handoff pass.

## Do not do next

- Do not build a decorative timeline or fake layer controls.
- Do not create a separate preview compositor and export compositor.
- Do not serialize `AVAsset`, `MTLTexture`, `UIImage`, SwiftUI state, security-scoped URL objects, or absolute sandbox paths.
- Do not weaken exact time, explicit color, deterministic persistence, recovery, license traceability, or preview/output parity.
- Do not claim continuous playback, video export, motion animation, tracking, AI cutout, shape or text engines, professional color or audio, nodes, particles, or 3D until implemented and tested.
- Do not copy proprietary application or plugin code, algorithms, presets, names, or assets.
