# Session Handoff

## Read this first

A new session must read, in order:

1. `README.md`
2. `Documentation/PRODUCT_VISION.md`
3. `Documentation/ENGINE_INVARIANTS.md`
4. `Documentation/ROADMAP_28_PHASES.md`
5. `Documentation/SOURCE_ADOPTION_MATRIX.md`
6. `Documentation/CORE_ARCHITECTURE.md`
7. `Documentation/BRANDING_AND_FIRST_RUN.md`
8. `Documentation/WORK_LOG.md`
9. the newest file under `docs/superpowers/plans/`
10. the latest open pull requests and GitHub Actions results

## Current state

- Repository: `woo642778-art/After-Effects`
- Phase 1 branch: `agent/phase-1-source-audit`
- Active Phase 2 branch: `agent/phase-2-core-architecture-branding`
- Current program: Phase 2 of 28
- Installed display name: `After Effects`
- Attribution: `Made by Maze`
- Current app: branded SwiftUI milestone and architecture shell with startup loading and first-install Telegram promotion
- Current reusable module: `VertexCore`
- Current binary policy: unsigned IPA artifact generated by GitHub Actions
- Production editor features completed: none

## Core contracts implemented

- `RationalTime` for exact normalized timeline values and explicit rescaling.
- `VertexID` for stable canonical project identity.
- `VertexPoint`, `VertexSize`, `VertexCoordinateSpace`, and `CoordinateConverter` for explicit geometry boundaries.
- `ColorDescriptor` for explicit primaries, transfer, matrix, and alpha metadata.
- `DependencyGraph` and `DependencyCycle` for deterministic ordering and cycle failure.
- `VertexError` for structured subsystem diagnostics.
- `OneTimePresentationGate` for deterministic first-run behavior.

## Product identity implemented

- The app display name is `After Effects`.
- The bundle identifier is `com.woo642778.aftereffects`.
- The supplied Ae image is stored as base64 source data and deterministically converted into AppIcon and LaunchLogo variants by `Tools/generate_app_assets.sh`.
- Startup displays the supplied logo, `After Effects`, `Made by Maze`, and loading progress.
- The Telegram promotion opens `https://t.me/aemotionios` and appears once per installation through `afterEffects.didPresentTelegramPromotion.v1`.

## Decisions already made

- Internal engine modules may keep the `Vertex` namespace while the installed product uses the After Effects identity.
- MetalPetal is the leading GPU-processing candidate, hidden behind a Vertex adapter.
- VideoIO is the leading AVFoundation preview/export utility candidate, hidden behind an adapter.
- VideoLab is a design reference, not the core time or project engine.
- MiniCut is GPL-3.0 and may be studied for behavior only unless the licensing strategy changes.
- Preview and export must share the same semantic render graph.
- Exact time, recoverable persistence, explicit color metadata, and license traceability cannot be weakened for speed.
- Commercial effect suites such as Sapphire and BCC are behavior and quality references only; the project will implement independent algorithms and names.

## Immediate next actions

1. Inspect the latest Phase 2 GitHub Actions run for the active branch.
2. Fix only evidence-backed Swift, asset-generation, XcodeGen, or packaging failures.
3. Confirm `AfterEffects.app` contains an arm64 Mach-O executable and compiled `Assets.car`.
4. Download the final unsigned Phase 2 IPA and verify its internal paths, display name, and SHA-256.
5. Record the workflow run ID, artifact ID, archive digest, IPA checksum, and final commit in `WORK_LOG.md`.
6. Open or update a stacked Draft PR from the Phase 2 branch to `agent/phase-1-source-audit`.
7. Do not begin Phase 3 until the Phase 2 remote build evidence is recorded.

## Do not do next

- Do not start a decorative editor UI.
- Do not add media import or AVFoundation code inside `VertexCore`.
- Do not import large external repositories into the product target.
- Do not add MetalPetal or VideoIO before adapter protocols and spike acceptance tests are written.
- Do not replace `RationalTime` with `Double` seconds.
- Do not use unlabelled coordinate or color values.
- Do not show the Telegram promotion repeatedly or block access when the user declines.
- Do not claim AE, Resolve, Alight Motion, Node Video, or Blurrr feature parity based on startup or milestone screens.
