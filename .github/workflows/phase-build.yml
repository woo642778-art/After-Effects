name: Phase Build and Unsigned IPA

on:
  push:
    branches:
      - main
      - "agent/**"
  pull_request:
  workflow_dispatch:

concurrency:
  group: phase-build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  core-tests:
    name: VertexCore tests
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: swift-actions/setup-swift@v2
        with:
          swift-version: "6.0"
      - name: Run deterministic core tests
        run: swift test

  unsigned-ipa:
    name: Build unsigned IPA
    runs-on: macos-15
    needs: core-tests
    steps:
      - uses: actions/checkout@v4

      - name: Select Xcode
        run: sudo xcode-select -s /Applications/Xcode.app

      - name: Install XcodeGen
        run: brew install xcodegen

      - name: Generate supplied icon assets
        run: bash Tools/generate_app_assets.sh

      - name: Generate Xcode project
        run: xcodegen generate

      - name: Build iOS application without signing
        run: |
          set -euo pipefail
          xcodebuild \
            -project Vertex.xcodeproj \
            -scheme Vertex \
            -configuration Release \
            -sdk iphoneos \
            -destination "generic/platform=iOS" \
            -derivedDataPath "$PWD/DerivedData" \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGNING_REQUIRED=NO \
            CODE_SIGN_IDENTITY="" \
            clean build

      - name: Verify product identity and architecture
        run: |
          set -euo pipefail
          APP_PATH="$PWD/DerivedData/Build/Products/Release-iphoneos/AfterEffects.app"
          test -d "$APP_PATH"
          test -f "$APP_PATH/Assets.car"
          DISPLAY_NAME="$(/usr/libexec/PlistBuddy -c 'Print :CFBundleDisplayName' "$APP_PATH/Info.plist")"
          test "$DISPLAY_NAME" = "After Effects"
          /usr/bin/file "$APP_PATH/AfterEffects" | /usr/bin/grep -q "arm64"

      - name: Package unsigned IPA and checksum
        run: |
          set -euo pipefail
          APP_PATH="$PWD/DerivedData/Build/Products/Release-iphoneos/AfterEffects.app"
          rm -rf Payload artifacts
          mkdir -p Payload artifacts
          cp -R "$APP_PATH" Payload/AfterEffects.app
          /usr/bin/zip -qry artifacts/After-Effects-Phase-2-unsigned.ipa Payload
          shasum -a 256 artifacts/After-Effects-Phase-2-unsigned.ipa > artifacts/After-Effects-Phase-2-unsigned.ipa.sha256

      - name: Upload unsigned IPA
        uses: actions/upload-artifact@v4
        with:
          name: After-Effects-Phase-2-unsigned-ipa
          path: artifacts/*
          if-no-files-found: error
          retention-days: 30
