name: Apply Vertex2 16 Version

on:
  push:
    branches:
      - agent/phase16-precomp-effects-performance
    paths:
      - .github/workflows/phase16-version-apply.yml

permissions:
  contents: write

jobs:
  apply-version:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: agent/phase16-precomp-effects-performance
          fetch-depth: 0
      - name: Update V16 identity
        run: |
          set -euo pipefail
          python3 - <<'PY'
          from pathlib import Path

          project = Path('project.yml')
          text = project.read_text()
          text = text.replace('MARKETING_VERSION: 15.0.0', 'MARKETING_VERSION: 16.0.0', 1)
          text = text.replace('CURRENT_PROJECT_VERSION: 15', 'CURRENT_PROJECT_VERSION: 16', 1)
          if 'MARKETING_VERSION: 16.0.0' not in text or 'CURRENT_PROJECT_VERSION: 16' not in text:
              raise SystemExit('Unable to establish Vertex2 16 project identity')
          project.write_text(text)

          schema = Path('Sources/VertexProject/ProjectSchema.swift')
          text = schema.read_text()
          text = text.replace('currentAppVersion = "15.0.0"', 'currentAppVersion = "16.0.0"', 1)
          if 'currentAppVersion = "16.0.0"' not in text:
              raise SystemExit('Unable to establish Vertex2 16 app contract')
          schema.write_text(text)
          PY
          git diff --check
      - name: Commit V16 identity
        run: |
          set -euo pipefail
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add project.yml Sources/VertexProject/ProjectSchema.swift
          if ! git diff --cached --quiet; then
            git commit -m 'chore: set Vertex2 version to 16.0.0'
            git push origin HEAD:agent/phase16-precomp-effects-performance
          fi
